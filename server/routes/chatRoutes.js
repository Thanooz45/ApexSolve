import {Router} from 'express';import {protect} from '../middleware/auth.js';import {imageUpload,audioUpload} from '../middleware/upload.js';import * as c from '../controllers/chatController.js';const r=Router();r.use(protect);r.get('/stats',c.stats);r.route('/').get(c.list).post(c.create);r.route('/:id').get(c.get).delete(c.remove);r.post('/:id/text',c.text);r.post('/:id/image',imageUpload,c.image);r.post('/:id/voice',audioUpload,c.voice);export default r;
