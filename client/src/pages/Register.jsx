import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import toast from 'react-hot-toast';import {useAuth} from '../context/AuthContext';import LoadingSpinner from '../components/common/LoadingSpinner';const options=['Mathematics','Physics','Chemistry','Biology','English','Computer Science','History'];export default function Register(){const [f,setF]=useState({name:'',email:'',password:'',grade:'10th',subjects:[]}),[busy,setBusy]=useState(false),{register}=useAuth(),nav=useNavigate();const toggle=s=>setF({...f,subjects:f.subjects.includes(s)?f.subjects.filter(x=>x!==s):[...f.subjects,s]});const submit=async e=>{e.preventDefault();setBusy(true);try{await register(f);nav('/dashboard')}catch(e){toast.error(e.response?.data?.message||'Could not create account.')}finally{setBusy(false)}};return <main className="auth"><form onSubmit={submit} className="auth-card"><div className="eyebrow">Your learning profile</div><h1>Let’s get acquainted.</h1><label>Name<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Email<input required type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><div className="row"><label>Grade<select value={f.grade} onChange={e=>setF({...f,grade:e.target.value})}>{['8th','9th','10th','11th','12th','College','Other'].map(x=><option key={x}>{x}</option>)}</select></label><label>Password<input required minLength="6" type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label></div><span className="label">Subjects you enjoy</span><div className="chips">{options.map(s=><button type="button" className={f.subjects.includes(s)?'chip selected':'chip'} onClick={()=>toggle(s)} key={s}>{s}</button>)}</div><button className="primary wide" disabled={busy}>{busy?<LoadingSpinner/>:'Create account'}</button><p>Already registered? <Link to="/login">Sign in</Link></p></form></main>}
